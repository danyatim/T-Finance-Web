# Инструкция по настройке на сервере

## 📋 Содержание
1. [Создание .env файла](#1-создание-env-файла)
2. [Генерация JWT ключа](#2-генерация-jwt-ключа)
3. [Создание Data Protection сертификата](#3-создание-data-protection-сертификата)
4. [Структура директорий](#4-структура-директорий)
5. [Проверка настройки](#5-проверка-настройки)

---

## 1. Создание .env файла

### Где создать:
Файл `.env` должен находиться **в корне проекта**, рядом с `docker-compose.yml`:

```
T-Finance-Web/
├── docker-compose.yml
├── .env                    ← ЗДЕСЬ
├── T-FinanceBackend/
├── T-FinanceFrontend/
└── ...
```

### Как создать:

```bash
# Перейдите в директорию проекта
cd /path/to/T-Finance-Web

# Скопируйте пример файла
cp .env.example .env

# Откройте для редактирования
nano .env
# или
vim .env
```

### Содержимое .env:

```bash
# JWT настройки (ОБЯЗАТЕЛЬНО)
JWT_KEY=ваш-сгенерированный-ключ-здесь
JWT_ISSUER=TFinanceBackend
JWT_AUDIENCE=TFinanceFrontend
JWT_EXPIRES_IN_HOURS=1

# Data Protection Certificate (ОПЦИОНАЛЬНО)
DATA_PROTECTION_CERT_PATH=/app/certs/dataprotection.pfx
DATA_PROTECTION_CERT_PASSWORD=ваш-пароль-сертификата
```

### Безопасность:
```bash
# Установите правильные права доступа
chmod 600 .env

# Убедитесь, что .env в .gitignore
grep -q "^\.env$" .gitignore || echo ".env" >> .gitignore
```

---

## 2. Генерация JWT ключа

### Вариант 1: Использование скрипта (рекомендуется)

```bash
# Сделайте скрипт исполняемым
chmod +x scripts/generate-jwt-key.sh

# Запустите скрипт
./scripts/generate-jwt-key.sh
```

Скрипт выведет сгенерированный ключ. Скопируйте его в `.env` как значение `JWT_KEY`.

### Вариант 2: Ручная генерация

```bash
# Генерация ключа длиной 64 символа
openssl rand -base64 64
```

Скопируйте результат в `.env`:

```bash
JWT_KEY=<результат-команды>
```

### Требования к ключу:
- Минимум 32 символа (рекомендуется 64+)
- Должен быть случайным и безопасным
- **ВАЖНО**: Сохраните ключ в безопасном месте! При потере все токены станут недействительными.

---

## 3. Создание Data Protection сертификата

### Зачем нужен:
Сертификат шифрует ключи Data Protection, которые используются для защиты cookies и других чувствительных данных.

### Вариант 1: Использование скрипта (рекомендуется)

```bash
# Сделайте скрипт исполняемым
chmod +x scripts/generate-dataprotection-cert.sh

# Запустите скрипт (с паролем или без)
./scripts/generate-dataprotection-cert.sh "ваш-пароль"
# или без пароля (скрипт сгенерирует случайный)
./scripts/generate-dataprotection-cert.sh
```

Скрипт создаст:
- `./certs/dataprotection.pfx` - сертификат в формате PFX
- Выведет пароль (если был сгенерирован автоматически)

### Вариант 2: Ручная генерация

```bash
# Создайте директорию для сертификатов
mkdir -p certs

# Генерируем приватный ключ и сертификат
openssl req -x509 -newkey rsa:2048 -keyout certs/dataprotection.key \
    -out certs/dataprotection.crt -days 3650 -nodes \
    -subj "/CN=TFinanceDataProtection/O=TFinance/C=RU"

# Конвертируем в PFX формат
openssl pkcs12 -export -out certs/dataprotection.pfx \
    -inkey certs/dataprotection.key \
    -in certs/dataprotection.crt \
    -passout "pass:ваш-пароль" \
    -name "TFinanceDataProtection"

# Удаляем временные файлы
rm certs/dataprotection.key certs/dataprotection.crt

# Устанавливаем права доступа
chmod 600 certs/dataprotection.pfx
```

### Настройка в .env:

```bash
# Если сертификат с паролем
DATA_PROTECTION_CERT_PATH=/app/certs/dataprotection.pfx
DATA_PROTECTION_CERT_PASSWORD=ваш-пароль

# Если сертификат без пароля (не рекомендуется)
DATA_PROTECTION_CERT_PATH=/app/certs/dataprotection.pfx
DATA_PROTECTION_CERT_PASSWORD=
```

### Где хранить на сервере:

Сертификат должен находиться в директории `./certs/` относительно `docker-compose.yml`:

```
T-Finance-Web/
├── docker-compose.yml
├── .env
├── certs/                          ← ЗДЕСЬ
│   └── dataprotection.pfx
├── T-FinanceBackend/
└── ...
```

### Безопасность:
```bash
# Установите правильные права доступа
chmod 600 certs/dataprotection.pfx
chmod 700 certs/

# Убедитесь, что certs/ в .gitignore
grep -q "^certs/" .gitignore || echo "certs/*.pfx" >> .gitignore
```

### Примечание:
- Сертификат опционален. Если не указан, ключи будут храниться в незашифрованном виде (менее безопасно).
- Сертификат монтируется в контейнер как read-only (`:ro`).
- Срок действия сертификата: 10 лет (3650 дней).

---

## 4. Структура директорий

После настройки структура должна выглядеть так:

```
T-Finance-Web/
├── docker-compose.yml
├── .env                          ← Секреты (НЕ в git!)
├── .env.example                  ← Пример (можно в git)
├── certs/                        ← Сертификаты (НЕ в git!)
│   └── dataprotection.pfx
├── ssl/                          ← SSL сертификаты для nginx
│   ├── fullchain.pem
│   └── privkey.pem
├── T-FinanceBackend/
│   ├── appsettings.json          ← Может содержать дефолтные значения
│   └── ...
├── T-FinanceFrontend/
└── scripts/
    ├── generate-jwt-key.sh
    └── generate-dataprotection-cert.sh
```

---

## 5. Проверка настройки

### Проверка .env файла:

```bash
# Проверьте, что файл существует
test -f .env && echo "✅ .env существует" || echo "❌ .env не найден"

# Проверьте права доступа
ls -la .env
# Должно быть: -rw------- (600)

# Проверьте содержимое (НЕ показывайте вывод в логах!)
grep -q "JWT_KEY=" .env && echo "✅ JWT_KEY настроен" || echo "❌ JWT_KEY не найден"
```

### Проверка сертификата:

```bash
# Проверьте, что сертификат существует
test -f certs/dataprotection.pfx && echo "✅ Сертификат существует" || echo "⚠️  Сертификат не найден (опционально)"

# Проверьте права доступа
ls -la certs/dataprotection.pfx
# Должно быть: -rw------- (600)
```

### Запуск приложения:

```bash
# Запустите контейнеры
docker compose up -d --build

# Проверьте логи backend
docker compose logs backend | grep -E "(JWT|DataProtection)"

# Должны увидеть:
# [DataProtection] Certificate loaded successfully from '/app/certs/dataprotection.pfx'
# (если сертификат настроен)
```

### Проверка работы:

```bash
# Проверьте, что backend запустился без ошибок
docker compose ps backend

# Проверьте логи на ошибки
docker compose logs backend --tail=50 | grep -i error
```

---

## 🔒 Безопасность

### ✅ Что нужно сделать:

1. **Никогда не коммитьте .env в git**
   ```bash
   # Проверьте .gitignore
   cat .gitignore | grep -E "^\.env$|^certs/"
   ```

2. **Используйте сильные пароли и ключи**
   - JWT_KEY: минимум 64 символа
   - Пароль сертификата: минимум 16 символов

3. **Ограничьте доступ к файлам**
   ```bash
   chmod 600 .env
   chmod 600 certs/*.pfx
   chmod 700 certs/
   ```

4. **Регулярно обновляйте ключи**
   - При компрометации немедленно смените все ключи
   - При смене ключей все существующие токены станут недействительными

5. **Делайте резервные копии**
   - Сохраните .env и пароли в безопасном месте (менеджер паролей)
   - НЕ храните в открытом виде

---

## 📝 Чеклист перед запуском

- [ ] Создан файл `.env` в корне проекта
- [ ] Сгенерирован и добавлен `JWT_KEY` в `.env`
- [ ] Настроены `JWT_ISSUER` и `JWT_AUDIENCE` в `.env`
- [ ] (Опционально) Создан Data Protection сертификат
- [ ] (Опционально) Настроены `DATA_PROTECTION_CERT_PATH` и `DATA_PROTECTION_CERT_PASSWORD` в `.env`
- [ ] Установлены правильные права доступа (600) на `.env` и сертификаты
- [ ] `.env` и `certs/` добавлены в `.gitignore`
- [ ] Проверены логи backend при запуске

---

## 🆘 Решение проблем

### Ошибка: "JWT Key не настроен"
**Решение**: Убедитесь, что `JWT_KEY` указан в `.env` файле.

### Ошибка: "Certificate file not found"
**Решение**: 
- Проверьте путь в `DATA_PROTECTION_CERT_PATH`
- Убедитесь, что файл существует в `./certs/`
- Проверьте, что volume смонтирован в `docker-compose.yml`

### Ошибка: "Failed to load certificate"
**Решение**:
- Проверьте пароль в `DATA_PROTECTION_CERT_PASSWORD`
- Убедитесь, что сертификат в формате PFX
- Проверьте права доступа к файлу

---

## 📚 Дополнительные ресурсы

- [Документация Docker Compose env_file](https://docs.docker.com/compose/environment-variables/env-file/)
- [ASP.NET Core Data Protection](https://learn.microsoft.com/en-us/aspnet/core/security/data-protection/)
- [JWT Best Practices](https://datatracker.ietf.org/doc/html/rfc8725)

