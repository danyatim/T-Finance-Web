# ============================================
# Конфигурация T-Finance Backend
# ============================================
# 
# ИНСТРУКЦИЯ:
# 1. Скопируйте этот файл в .env в корне проекта (рядом с docker-compose.yml)
#    cp env.template .env
# 2. Заполните все значения
# 3. НИКОГДА не коммитьте .env в git!
#
# ============================================

# JWT настройки (ОБЯЗАТЕЛЬНО)
# Генерируйте безопасный ключ минимум 32 символа (рекомендуется 64+)
# Можно использовать: openssl rand -base64 64
JWT_KEY=your-very-long-and-secure-secret-key-here-minimum-32-characters-recommended-64-or-more
JWT_ISSUER=TFinanceBackend
JWT_AUDIENCE=TFinanceFrontend
JWT_EXPIRES_IN_HOURS=1

# Data Protection Certificate (ОПЦИОНАЛЬНО)
# Путь к сертификату внутри контейнера (будет монтироваться из ./certs/)
# Если не указан, ключи будут храниться в незашифрованном виде
DATA_PROTECTION_CERT_PATH=/app/certs/dataprotection.pfx
DATA_PROTECTION_CERT_PASSWORD=your-certificate-password-here

# YooKassa настройки (ОБЯЗАТЕЛЬНО для работы платежей)
# Получите эти данные в личном кабинете YooKassa: https://yookassa.ru/
YOOKASSA_SHOP_ID=your-shop-id-here
YOOKASSA_SECRET_KEY=your-secret-key-here
YOOKASSA_RETURN_URL=https://t-finance-web.ru/

# Premium подписка цена (ОПЦИОНАЛЬНО)
# Цена в рублях (по умолчанию 999.00)
PREMIUM_PRICE=999.00

# SMTP настройки для отправки писем (ОБЯЗАТЕЛЬНО для верификации email)
# Яндекс почта SMTP настройки
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=587
SMTP_USERNAME=your-email@yandex.ru
SMTP_PASSWORD=your-app-password
SMTP_FROM_EMAIL=your-email@yandex.ru
SMTP_FROM_NAME=T-Finance

# URL приложения (ОПЦИОНАЛЬНО)
# Базовый URL бэкенда для формирования ссылок подтверждения
APP_BASE_URL=https://t-finance-web.ru
# URL фронтенда для редиректа после подтверждения
FRONTEND_URL=https://t-finance-web.ru

# ============================================
# ПРИМЕЧАНИЯ:
# - JWT_KEY должен быть длинным и случайным
# - DATA_PROTECTION_CERT_PASSWORD можно оставить пустым, если сертификат без пароля
# - YOOKASSA_SHOP_ID и YOOKASSA_SECRET_KEY можно получить в личном кабинете YooKassa
# - YOOKASSA_RETURN_URL - URL, на который пользователь вернется после оплаты
# - Все значения чувствительны к регистру
# ============================================

